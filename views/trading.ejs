<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tuxedo Trades</title>
  <link rel="stylesheet" href="/css/global.css">
  <link rel="stylesheet" href="/css/trading.css">
  <link rel="stylesheet" href="/css/history.css">
</head>
<body> <!-- page requires extentions to change based on displayed stock -->
  <%- include('../blocks/nav') %>
  <div class="tradetable">
    <h1>Available Stocks</h1>
    <table id="historyTable">
      <thead>
        <tr>
          <th>Ticker</th>
          <th class="m-h1">Company</th>
          <th>Current Price</th>
          <th class="m-h2">Volume</th>
          <th class="m-h2">Market Cap</th>
          <th class="m-h1">Opening Price</th>
          <th class="m-h3">High</th>
          <th class="m-h3">Low</th>
          <th>Day Change</th>
          <th>Buy / Sell</th>
        </tr>
      </thead>
      <tbody>
        <% stocks.forEach(stock => { %>
          <tr data-ticker="<%= stock.ticker %>">
            <td><%= stock.ticker %></td>
            <td class="m-h1"><%= stock.company %></td>
            <td>$<%= stock.history.at(-1)?.price.toLocaleString() %></td>
            <td class="m-h2"><%= stock.volume %></td>
            <td class="m-h2"><%= stock.marketCap.toLocaleString() %></td>
            <td class="opening m-h1">-</td>
            <td class="high m-h3">-</td>
            <td class="low m-h3">-</td>
            <td class="change">-</td>
            <td class="buysell">
              <button><a href='/<%= user.username %>/trading/<%= stock.ticker %>?action=buy'>BUY</a></button>
              <button><a href='/<%= user.username %>/trading/<%= stock.ticker %>?action=sell'>SELL</a></button>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
  <%- include('../blocks/footer') %>
  <script src="/js/dashboard.js"></script>
  <script>
    window.addEventListener('DOMContentLoaded', () => {
      displayAllDayChanges();
    });
  </script>
</body>
</html>
